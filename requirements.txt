# Short-Swing Stock Signal MCP server
# MCP SDK — pin to v1 (v2 changes the transport layer and is still in development).
mcp[cli]>=1.25,<2

# Market data
yfinance>=0.2.40

# News + sentiment
feedparser>=6.0.11
vaderSentiment>=3.3.2

# HTTP + env
httpx>=0.27
python-dotenv>=1.0

# Data / indicators
pandas>=2.0
numpy>=1.26
# pandas-ta is OPTIONAL and intentionally NOT listed here: current releases require
# Python >=3.12, and indicators.py hand-rolls every indicator as a fallback. On
# Python 3.12+ you may install it for the (identical) pandas-ta engine:
#   uv pip install "pandas-ta>=0.3.14b0"
